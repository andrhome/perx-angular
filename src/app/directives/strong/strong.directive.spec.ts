import { StrongDirective } from './strong.directive';

describe('StrongDirective', () => {
  it('should create an instance', () => {
    const directive = new StrongDirective();
    expect(directive).toBeTruthy();
  });
});
